import 'package:flutter/material.dart';

class CoursPopular extends StatelessWidget {
  const CoursPopular({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
